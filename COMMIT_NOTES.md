- fix(analysis): the duration quantum is now resolved from the recording rather than assumed. Decoders had defaulted to 0.25 ms, which is correct for firmware that emits timecode frames but wrong for every recording made before them — those failed to lock with "no part of this recording matches 6 h of the generator's output", which reads as bad data when it is a configuration mismatch. score() now asks the recording to declare itself: a decodable timecode frame means the full variant and the current quantum; no frame means the simple variant, which predates the finer quantum and is always 5 ms. An explicit step_ms= still wins. The resolved value and the reason are carried on the report (2026-09-13T18:30:00-04:00)
- fix(analysis): classify() rebuilt the template and derived the gross-vs-jitter threshold from the caller's step_ms argument rather than the value score() had actually locked on. With no argument passed that silently used a different quantum than the lock, so the template no longer matched the waveform and the gross cut was scaled wrong. Both now read the resolved value off the report (2026-09-13T18:30:00-04:00)
- fix(analysis): timecode frames were never decoded from a both-edges stream. decode_frames needs a single polarity — an interleaved list halves every interval and matches no preamble — but analyze_streams passed the stream through unchanged, behind a conditional whose two branches were identical and a bare except that discarded the failure. It now tries each polarity and keeps whichever yields more valid frames, as the align path already did, and a decode error is reported instead of swallowed (2026-09-13T18:30:00-04:00)
- feat(analysis): every report states which quantum it used and how that was determined, before any number. Getting the quantum wrong does not degrade a result, it prevents a lock, so the assumption is visible on every run rather than only when something fails. A frameless recording additionally prints how to override. A failed lock now names the quantum tried, the reason it was chosen, and the alternative to retry with (2026-09-13T18:30:00-04:00)
- fix(report): the on-tick line hardcoded "the correct 5 ms tick" regardless of the quantum in use. It now renders the resolved value (2026-09-13T18:30:00-04:00)
- fix(docs): the utils README claimed everything exists in MATLAB, Python and R with matching behaviour, and described the gaps as missing convenience wrappers. Several are whole capabilities with no equivalent to call: the offset/drift/jitter/loss decomposition, the one-call analysis and report, template-free pairwise diagnosis, signal fidelity checking, XDF/LSL loading, and the event-channel decoder — which the function table did not list at all, so a reader choosing a language could not see what they would give up. Added a Python-only table and stated plainly that a dash means absent, not unwrapped (2026-09-13T18:30:00-04:00)
- fix(tests): the synthetic fixtures built their edge trains at the library default quantum and then relied on the decoder defaulting to the same value. With the quantum now resolved from the signal, a frameless synthetic stream is read as the simple variant, so the fixtures declare their own quantum explicitly. This is the honest form: a generated stream carries no protocol and cannot be asked (2026-09-13T18:30:00-04:00)

<!-- version-bumps
[
  { "tool": "PRESYNC", "bump": "minor", "reason": "quantum resolved from the recording; both-edges frame decoding fixed; reports state their assumptions" }
]
-->
